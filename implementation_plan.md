# バーWebサイトデモ実装計画

リファレンスサイト「Bar Mulan」を参考に、架空のバー「Bar Demo」のデモサイトを作成します。
高級感のあるダークなデザインを採用した、レスポンシブ対応のシングルページ構成となります。

## ユーザー確認事項

> [!IMPORTANT]
> 著作権の問題を避けるため、ヒーローセクションや店内の雰囲気画像には、リファレンスサイトのものをそのまま使わず、AI生成画像を使用します。

## 変更内容の提案

### プロジェクト構成 (新規)
`bar-demo/` ディレクトリ配下に以下のファイルを作成します。

#### [NEW] index.html
- セマンティックなHTML5構造を採用します。
- **セクション構成**:
    - **Hero**: フルスクリーン画像とキャッチコピー（例：「あなたの日常にちょっとした特別を」）。
    - **About**: お店のコンセプト紹介。
    - **System**: チャージ料金、税、サービス料の案内。
    - **Menu**: おすすめメニュー（カクテル、ウイスキー等）のハイライト。
    - **Access**: 住所表示と地図（プレースホルダーまたは埋め込み）。
    - **Footer**: SNSリンク、コピーライト、主要リンク。

#### [NEW] style.css
- **デザインテーマ**:
    - ベースカラー: ダークグレーまたは深いミッドナイトブルー (`#1a1a1a`)
    - アクセントカラー: ゴールド (`#c5a059`)
    - テキスト: 白 (`#ffffff`)
- **タイポグラフィ**:
    - 見出し: 上品なセリフ体 (Google Fonts 'Playfair Display' や 'Shippori Mincho' 等)
    - 本文: 読みやすいサンセリフ体
- **レスポンシブ**: モバイルファーストで設計し、スマホでもPCでも美しく表示します。

#### [NEW] script.js
- ハンバーガーメニュー（モバイル用ナビゲーション）の開閉動作。
- ページ内リンクへのスムーススクロール。
- スクロール時のフェードインアニメーション（Intersection Observer API）。

### アセット (画像生成)
`generate_image` ツールを使用して、オリジナルの素材を作成します。
- `hero_bar.webp`: 照明を落とした、ムードのある高級バーカウンターの画像。
- `cocktail_signature.webp`: シグネチャーカクテルのアップ画像。

## 検証計画

### 自動テスト
- **ブラウザ確認**: ブラウザツールを使用して `index.html` を開き、表示崩れがないか確認します。
- **コンソール確認**: JavaScriptのエラーが出ていないかチェックします。

### 手動検証
- 作成完了後、ファイルのフルパスを提示しますので、ご自身のブラウザで開いて動作（動き、レスポンシブ挙動）をご確認ください。
